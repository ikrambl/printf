#ifndef MAIN_H
#define MAIN_H
#include <stdlib.h>
#include <stdarg.h>
#include <stdio.h>
#include <unistd.h>

int k_putchar(char c);
void k_putint(int n);
void k_putstr(char *str);
int _printf(const char *format, ...);
int _putchar(char c);

#endif
