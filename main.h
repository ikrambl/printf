#ifndef MAIN_H
#define MAIN_H
#include <stdlib.h>
#include <stdarg.h>
#include <stdio.h>
#include <unistd.h>

void k_putchar(int c);
void k_putint(int n);
void k_putstr(char *str);
int _printf(const char *format, ...);

#endif
